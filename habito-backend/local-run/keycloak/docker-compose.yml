# docker-compose.yml
version: '3.1'
services:
  habito-keycloak:
    image: quay.io/keycloak/keycloak:24.0.3
    container_name: habito-keycloak
    command: start-dev
    ports:
      - 8080:8080
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: hadmin